# Optional POST endpoint accepting { formId, page, fields } as JSON.
# Leave empty to validate locally without sending any data.
# PUBLIC_ values are visible in the browser; never put secrets here.
PUBLIC_FORM_ENDPOINT=
